import SwiftUI

struct PuzzlesView: View {
    var body: some View {
        Text("Puzzles Management")
            .navigationTitle("Puzzles")
    }
}

struct TeamsView: View {
    var body: some View {
        Text("Teams Management")
            .navigationTitle("Teams")
    }
}

struct DashboardView: View {
    var body: some View {
        Text("Live Dashboard")
            .navigationTitle("Dashboard")
    }
}
